# CasaOS Deployment (Draft)

_Status: outline — see [`docs/backlog.md`](../backlog.md#active-initiatives) for remaining work._

## Goal
Document how to deploy ColmenaOS on CasaOS using the unified `docker-compose.yml` bundle and CasaOS metadata.

## To Capture
- Required CasaOS metadata annotations (icon, category, env prompts).
- Steps to import the compose file via the CasaOS UI and configure secrets.
- Post-install verification and rollback instructions.

## Current References
- Compose metadata block in [`docs/30-implementation/docker-compose.md`](../30-implementation/docker-compose.md).
- Platform packaging tasks in [`docs/20-roadmap.md`](../20-roadmap.md#phase-3--platform-packaging-week-3).

Promote this doc from draft once the CasaOS integration tasks in the backlog are complete.
