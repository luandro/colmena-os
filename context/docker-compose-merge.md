# Docker Compose Notes (Scratch)

Authoritative compose guidance is tracked in [`../docs/30-implementation/docker-compose.md`](../docs/30-implementation/docker-compose.md).

Capture experiments, alternative port mappings, or CasaOS metadata drafts here. Move finalized decisions to the docs directory to keep a single source of truth.
